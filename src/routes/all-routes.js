const express = require("express");
//---------------------------------------------------------
//---------------------------------------------------------
const groupApi = require("./Group-Routes");
// const categoryApi = require("./Category-Routes");
//---------------------------------------------------------
const authApi = require("./Auth-routes");
//---------------------------------------------------------

//---------------------------------------------------------
const productApi = require("./File-Routes");
//---------------------------------------------------------
//---------------------------------------------------------
const buyerApi = require("./Buyer-Routes");
//---------------------------------------------------------
const router = express.Router();
//---------------------------------------------------------
router.get("/test", (req, res) => res.send("Working"));

//---------------------------------------------------------
router.use("/group", groupApi);
// router.use("/category", categoryApi);
//---------------------------------------------------------
router.use("/auth/buyer", authApi);
//---------------------------------------------------------

//---------------------------------------------------------
router.use("/product", productApi);

//---------------------------------------------------------
router.use("/buyer", buyerApi);
//---------------------------------------------------------
module.exports = router;
